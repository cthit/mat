version: "2"
services:
  redis:
    image: redis:5.0.3
    ports: 
      - "6379"
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: mat_frontend
    ports:
      - "3000:80"
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: mat_backend
    ports:
      - "8080:8080"

